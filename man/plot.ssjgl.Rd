% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/methods.R
\name{plot.ssjgl}
\alias{plot.ssjgl}
\title{Plot partial correlation heatmaps from an ssjgl fit}
\usage{
\method{plot}{ssjgl}(
  x,
  v0_index = NULL,
  threshold = 0.5,
  zlim = c(-1, 1),
  col = grDevices::hcl.colors(50, "Blue-Red 3"),
  ...
)
}
\arguments{
\item{x}{An object of class \code{ssjgl}.}

\item{v0_index}{Integer index into the v0 ladder. Default \code{NULL}
uses the last step.}

\item{threshold}{Numeric; edges with inclusion probability below this
value are masked. Default 0.5. Set to 0 to show all edges.}

\item{zlim}{Numeric vector of length 2 for the color scale. Default
\code{c(-1, 1)}.}

\item{col}{Color palette vector. Default uses \code{hcl.colors(50, "Blue-Red 3")}.}

\item{...}{Additional arguments passed to \code{\link[graphics]{image}}.}
}
\value{
Invisible list of K partial correlation matrices (after masking).
}
\description{
Produces a side-by-side heatmap of estimated partial correlations for
each group at the specified v0 step. Edges with inclusion probability
below \code{threshold} are masked (set to zero).
}
\examples{
sim <- simulate_ssjgl_data(K = 2, p = 10, n = 50, seed = 1)
fit <- ssjgl(sim$data_list, penalty = "fused",
             lambda0 = 1, lambda1 = 0.5, lambda2 = 0.5,
             v0s = 0.01, maxitr.em = 10, impute = FALSE)
plot(fit)
}
