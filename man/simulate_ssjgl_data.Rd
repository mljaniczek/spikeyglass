% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/utils-datagen.R
\name{simulate_ssjgl_data}
\alias{simulate_ssjgl_data}
\title{Simulate data from known precision matrices for multiple groups}
\usage{
simulate_ssjgl_data(
  K = 2,
  p = 20,
  n = 100,
  graph_type = c("band", "random", "hub", "scale-free"),
  edge_prob = 0.1,
  perturb_prob = 0.05,
  signal = 0.3,
  seed = NULL
)
}
\arguments{
\item{K}{Integer number of groups. Default 2.}

\item{p}{Integer number of variables. Default 20.}

\item{n}{Integer number of observations per group (scalar applied to all
groups, or vector of length K). Default 100.}

\item{graph_type}{Character string specifying the base graph type.
One of \code{"band"} (AR-2 banded), \code{"random"} (Erdos-Renyi),
\code{"hub"} (star topology), or \code{"scale-free"} (Barabasi-Albert).
Default \code{"band"}.}

\item{edge_prob}{Numeric edge probability for \code{"random"} graph type.
Default 0.1.}

\item{perturb_prob}{Numeric probability of perturbing (flipping) each edge
in groups 2, ..., K relative to group 1. Default 0.05.}

\item{signal}{Numeric signal strength for off-diagonal entries of the
precision matrix. Default 0.3.}

\item{seed}{Integer random seed for reproducibility. Default \code{NULL}.}
}
\value{
A list with elements:
\describe{
\item{data_list}{List of K data matrices, each n_k x p.}
\item{Omega_list}{List of K true precision matrices (p x p).}
\item{adj_list}{List of K binary adjacency matrices (p x p, 0 diagonal).}
\item{Sigma_list}{List of K true covariance matrices (p x p).}
\item{K}{Number of groups.}
\item{p}{Number of variables.}
\item{n}{Vector of sample sizes.}
\item{graph_type}{The graph type used.}
}
}
\description{
Generates synthetic data from K groups with shared and differential
graph structure. Useful for evaluating the performance of
\code{\link{ssjgl}}.
}
\examples{
sim <- simulate_ssjgl_data(K = 2, p = 10, n = 50, graph_type = "band", seed = 42)
str(sim, max.level = 1)
}
